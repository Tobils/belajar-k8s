apiVersion: v1
kind: Pod
metadata:
  name: pod-name
  labels:
    label-key1: key-1
  annotations:
    annotation-key1: annotation-value
    annotation-key2: very long annotation value, bla bli bil
spec:
  containers:
    - name: container-name
      image: image-name
      ports:
        - containerPort: 80
